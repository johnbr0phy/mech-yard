# Third-party reference art, kept locally for design reference only.
# This repo is public — nothing in here is ours to redistribute.
references/

# Tuning DATA is local; the tool that produces it belongs in the repo
tune/ratings.json
tune/.build.js
tune/sheets/
*.local.json
